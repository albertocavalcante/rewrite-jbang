<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder>
            <pattern>%cyan(%msg) %n</pattern>
        </encoder>
    </appender>

    <!-- Special colored appender for different log levels -->
    <appender name="COLOR_APPENDER" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder>
            <pattern>%magenta(%level{ERROR=ERROR, WARN=WARNING, INFO=, DEBUG=DEBUG})%blue(%logger) %red(ERROR:) %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>NEUTRAL</onMismatch>
        </filter>
    </appender>

    <!-- Custom patterns by level -->
    <appender name="WARN_APPENDER" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder>
            <pattern>%yellow(%msg) %n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>WARN</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <appender name="INFO_APPENDER" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder>
            <pattern>%green(%msg) %n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- Suppress ReloadableJava11Parser logs -->
    <logger name="org.openrewrite.java.isolated.ReloadableJava11Parser" level="ERROR" />
    
    <!-- Suppress most openrewrite logs -->
    <logger name="org.openrewrite" level="WARN" />

    <root level="INFO">
        <appender-ref ref="WARN_APPENDER" />
        <appender-ref ref="INFO_APPENDER" />
        <appender-ref ref="COLOR_APPENDER" />
    </root>
</configuration> 